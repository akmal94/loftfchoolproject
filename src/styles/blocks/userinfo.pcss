.userInfo {
  display: flex;
  //justify-content: center;
  margin-left: 165px;
  &__title {
    font-size: 18px;
    //color: rgba(255, 255, 255, 0.5);
    margin-right: 68px;
    font-weight: 600;
  }
  &__name {
    font-size: 72px;
    font-weight: 800;
  }
}
